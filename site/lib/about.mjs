// about.mjs — About 페이지 본문(한글 markdown).
//
// 내용은 저장소의 README.md / CLAUDE.md 에서 추출한 프로젝트 철학·THE FOUR RULES·
// 3-tier 파이프라인을 요약·정리한 것이다 (Rule #1 위반 아님 — 로컬 문서 읽기).
// build.mjs 가 이 문자열을 renderMarkdown 으로 렌더해 about() 템플릿에 넘긴다.
//
// 본문 안의 [[category]] 위키링크는 resolver 가 홈의 카테고리 밴드 앵커로 해석한다.

export const ABOUT_INTRO =
  'AI 관련 자료(논문 · 코드 · 아티클 · 리포트 · 영상 · 책 · 강의)를 한곳에 모아 두고, ' +
  '검색하거나 다시 찾아보기 쉽게 정리한 개인 지식 베이스예요. ' +
  'Karpathy의 LLM Wiki 방식을 여러 자료 유형으로 넓혔습니다.';

export function aboutMarkdown(hrefFn) {
  const home = hrefFn ? hrefFn('/') : '/';
  return `이 사이트는 제가 모아 둔 **AI 지식 베이스**를 웹과 모바일에서 편하게 읽으려고 만든
정적 페이지예요. 실제 내용은 \`wiki/\`, \`sources/\`, \`raw/\` 폴더에 한 벌로 보관하고,
이 페이지들은 빌드할 때 그걸 그대로 읽어 보여 주기만 합니다. 그래서 여기서는 내용을 고치지 않고 읽기만 해요.

목표는 하나예요. 매일 새 모델과 논문이 쏟아지는 와중에도, 자료가 쌓일수록 가치가 더 커지는
지식 베이스를 만드는 거예요. 새 자료를 넣을 때마다 기존 자료와 링크(\`[[wikilinks]]\`)로 이어지고,
여러 자료를 묶은 정리 페이지(overview)로 합쳐지면서 지식이 복리처럼 불어납니다.

## 네 가지 규칙 — 이 시스템의 심장

이 위키가 지키려는 건 딱 하나예요. **없는 내용을 지어내지 않고, 모든 답이 실제로 가지고 있는
자료에만 근거하도록 하는 것.** 이 네 규칙이 없으면 위키는 그냥 잘 차려입은 웹 검색기와 다를 게 없어요.

1. **웹 검색은 하지 않아요.** 모르는 부분을 \`WebSearch\`·\`WebFetch\`로 메우지 않습니다.
   가진 자료만으로 답하는 게 이 위키의 존재 이유예요.
2. **위키를 먼저 봐요.** \`sources/\`와 \`wiki/\`가 유일한 기준(진실의 원천)이에요.
3. **위키로 부족하면 원본을 다시 읽어요.** \`raw/\`의 원본 자료에서 필요한 내용을 더 뽑아내 위키를 보강합니다.
4. **자료가 없으면 없다고 말해요.** 억지로 채우지 않고, *"그 주제에 대한 자료가 아직 없어요 —
   원본 자료(PDF · 링크 · 자막 등)를 주세요"*라고 답합니다.

이 규칙은 정리 페이지(overview)를 포함해 **모든** 답변에 똑같이 적용돼요 — 위키에 있는 자료만 인용합니다.

## 자료가 정리되는 3단계

자료 하나는 아래 세 단계를 거쳐 다듬어져요. 세 단계의 파일은 **같은 이름(stem)**을 공유해서
서로 짝을 찾기 쉽습니다.

\`\`\`
원본 자료 (raw/{papers|repos|articles|reports|videos|books|lectures}/)
    ↓ 글과 그림(도식) 뽑아내기
sources/{stem}.md         (한글 요약 — 그림이 있으면 후보까지)
    ↓ 다듬기 + 자료끼리 연결 + 고른 그림 넣기
wiki/{category}/{stem}.md (한글, [[wikilinks]], ![[assets/...]])
    ↓ 여러 자료를 묶기
wiki/overviews/{topic}.md ← 지식이 복리로 쌓이는 곳
\`\`\`

1. **raw/** — 손대지 않는 원본 보관소예요. PDF · 코드 스냅샷 · 아티클 본문 · 자막 · 책 ·
   강의 패키지를 그대로 둡니다. 항상 복사해서 넣고, 바로가기(symlink)는 쓰지 않아요.
2. **sources/** — 원본을 한글로 요약한 단계예요. 그림은 본문에 넣지 않고 후보만 메모해 둡니다.
3. **wiki/** — 자료끼리 링크(\`[[wikilinks]]\`)로 잇고, 골라낸 그림(\`![[assets/...]]\`)을 넣은 완성 페이지예요.

진짜 가치가 불어나는 곳은 여러 자료를 한데 묶는 **정리 페이지(overview)**입니다.

## 자료 분류

자료의 유형(raw/의 폴더)과 위키의 분류(wiki/의 폴더)는 서로 따로 놀아요.
그래서 같은 분류 안에 논문 · 코드 · 아티클이 섞여 들어갈 수도 있어요.
분류는 주제가 아니라 **방법**을 기준으로 정합니다 — *"나중의 내가 어느 칸에서 찾으면 제일
빨리 발견할까?"* 가 판단 기준이에요.

지금 있는 분류는 이래요: [[database]] · [[llms]] · [[physical-ai]] · [[agents]] · [[evaluations]] ·
[[applications]] · [[etc]] · [[overviews]]. [홈으로 가면](${home}) 분류별 카드를 한눈에 볼 수 있어요.

## 둘러보는 방법

- **이 사이트에서**: 전체 검색(\`Ctrl/⌘ K\`), 라이트·다크 전환, 위키 페이지마다 목차와 관련 페이지를 볼 수 있어요.
- **Obsidian에서**: \`wiki/\` 폴더를 Vault로 열면 링크(\`[[wikilinks]]\`) · 그래프 뷰 · 전체 검색을 그대로 쓸 수 있어요.

더 자세한 규칙과 자료 추가 방법은 저장소의 \`README.md\`와 \`CLAUDE.md\`에 있어요.`;
}
