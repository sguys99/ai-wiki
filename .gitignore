# Python venv (uv로 생성, PDF 추출용)
.venv/
__pycache__/
*.pyc

# humanize-korean 스킬 작업 스크래치 (run별 산출물 — 추적 불필요)
_workspace/

# macOS
.DS_Store

# Editor
.idea/
.vscode/

# Obsidian (per-session UI state — noisy; 다른 .obsidian 설정은 기기 간 공유 유지)
.obsidian/workspace.json

# 웹 빌드 산출물 (GitHub Pages — site/build.mjs 출력, Actions가 배포)
dist/
site/node_modules/
# Pagefind 인덱스 (빌드 시 생성)
dist/pagefind/
